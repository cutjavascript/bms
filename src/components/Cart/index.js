import React from "react";
import PropTypes from "prop-types";

class Cart extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      hasError: false,
    };
  }

  render() {
    return <div>hi</div>;
  }
}

export default Cart;
